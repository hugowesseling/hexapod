commit:
git commit

add files:
git add <filename(s)>

push to github:
git push origin master

add files, commit and push to github:
./gacp (script: Git Add, Commit and Push)

pull from github:
git pull origin


==Initialization==

add github:
git remote add origin https://github.com/hugowesseling/hexapod.git


